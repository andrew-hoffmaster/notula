<!-- Thanks for contributing! Keep it focused — one change per PR where possible. -->

## What & why

<!-- What does this change, and why? Link any related issue (e.g. Closes #12). -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (added/updated tests for new behavior)
- [ ] `npm run build` succeeds
- [ ] No Node/Electron APIs used directly in the renderer (goes through `window.api`)
